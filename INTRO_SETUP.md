# הוראות הגדרת מערכת האינטרו

## קבצים שצריך להוסיף:

### 1. תמונות אינטרו
הוסף 5 תמונות לתיקייה `public/intro/`:
- `slide1.png` - תמונה ראשונה
- `slide2.png` - תמונה שנייה
- `slide3.png` - תמונה שלישית
- `slide4.png` - תמונה רביעית
- `slide5.png` - תמונה חמישית

**מומלץ:**
- גודל: 1920x1080 או יחס דומה (16:9)
- פורמט: PNG עם שקיפות (אם נדרש)
- איכות: גבוהה אבל לא כבד מדי (מומלץ עד 500KB לכל תמונה)

### 2. מוזיקת אינטרו
הוסף קובץ מוזיקה לתיקייה `public/`:
- `intro.mp3` - מוזיקת רקע לאינטרו

**מומלץ:**
- פורמט: MP3
- אורך: 15-30 שניות (או יותר אם רוצים)
- איכות: 128-192 kbps

### 3. לוגו
הוסף קובץ לוגו לתיקייה `public/`:
- `logo.png` - לוגו המשחק

**מומלץ:**
- גודל: 200x200px או יותר
- פורמט: PNG עם שקיפות (אם נדרש)

## שימוש:

### הפעלת אינטרו
האינטרו יוצג אוטומטית בפעם הראשונה שהמשתמש מתחיל משחק.
אם המשתמש דילג על האינטרו, הוא לא יוצג שוב (נשמר ב-localStorage).

### דילוג על אינטרו
- לחץ על כפתור "דלג" במסך האינטרו
- או המתן שהאינטרו יסתיים אוטומטית

### איפוס האינטרו (לצורך בדיקה)
פתח את Console בדפדפן והקלד:
```javascript
localStorage.removeItem('hasSeenIntro');
```
לאחר מכן רענן את הדף והאינטרו יוצג שוב.

## הערות טכניות:

- כל התמונות נטענות מראש (preloading) לפני הצגת האינטרו
- מוזיקת האינטרו נעצרת אוטומטית כשהאינטרו מסתיים
- המעבר בין תמונות הוא חלק עם אנימציית fade
- כל תמונה מוצגת למשך 4 שניות (ניתן לשנות ב-`IntroSlides.tsx`)
